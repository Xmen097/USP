#include "uploader.h"

/*

pin_t Uploader::resolve_pin_type(PinType pin) {
    if (!detector.chip_identified) error.except(Error::ChipDetect);
    if (pin == PinType::MOSI || pin == PinType::CLK || pin == PinType::MISO, pin == PinType::RESET) {
        error.except(Error::ChipDetect);
    } else {
        error.except(Error::Implementation);
    }
    return 0;
}

*/
